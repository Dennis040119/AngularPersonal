import { HttpHeaders } from "@angular/common/http"

export const GLOBAL ={
    URL_BASE:"http://localhost:8090/",
    temaColor:""
}

/*export const httpOptions = {
    headers: new HttpHeaders({
      'Authorization': 'Bearer ' + localStorage.getItem("token"), // Reemplaza 'tuToken' con el token real
      'Content-Type': 'application/json' // Esto depende del tipo de contenido que estás enviando
    })
  
}*/

// Construye manualmente el token Bearer


